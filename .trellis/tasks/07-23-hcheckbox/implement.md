# 执行计划：HCheckbox

1. tokens 增加 checkbox 尺寸/边框/勾选色
2. 实现 `HCheckbox.vue`（原生 checkbox + indeterminate 同步 + label）
3. `index.ts` 导出
4. playground：v-model、disabled、sizes、indeterminate 演示（宿主清半选）
5. README + component-guidelines / tokens / quality / index
6. `npm run build:playground`

## 验证

```bash
npm run build:playground
```
