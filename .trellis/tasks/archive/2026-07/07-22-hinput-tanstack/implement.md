# 执行计划：HInput

1. tokens 增加 input 高度/边框/invalid 色
2. 实现 `HInput.vue`
3. `index.ts` 导出
4. playground：v-model 段 + TanStack（或 mock）Field 段
5. README/spec 更新（含 TanStack 绑定示例）
6. `npm run build:playground`

## 验证

```bash
npm run build:playground
```
