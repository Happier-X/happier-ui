# 执行计划：HIcon

1. 根 `package.json` peer 增加 `@lucide/vue`；playground 安装
2. tokens 增加 `--h-icon-size-*`
3. 实现 `HIcon.vue`（:icon、variant stroke/fill、size、strokeWidth、color、ariaLabel）
4. `index.ts` 导出
5. playground：stroke/fill 对比、sizes、aria-label
6. README + component-guidelines / tokens / quality / index
7. `npm run build:playground`

## 验证

```bash
npm run build:playground
```
