# 执行计划：HTabBar

1. tokens 按需补 tab-bar bg/border/label
2. 实现 `HTabBar.vue`（items + v-model key + HIcon + safe-area）
3. `index.ts` 导出
4. playground：4 项切换 + 当前 key 回显
5. README + component-guidelines / tokens / quality / index
6. `npm run build:playground`

## 验证

```bash
npm run build:playground
```
